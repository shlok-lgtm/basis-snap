{
  "name": "basis-snap",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @basis-protocol/snap run build && yarn workspace @basis-protocol/site run build",
    "build:snap": "yarn workspace @basis-protocol/snap run build",
    "start": "yarn workspace @basis-protocol/snap run start",
    "test": "yarn workspace @basis-protocol/snap run test",
    "lint": "yarn workspace @basis-protocol/snap run lint && yarn workspace @basis-protocol/site run lint"
  },
  "devDependencies": {
    "typescript": "~5.3.3"
  },
  "engines": {
    "node": ">=18.6.0"
  }
}
